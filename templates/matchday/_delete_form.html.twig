<form method="post" action="{{ path('app_matchday_delete', {'id': matchday.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matchday.id) }}">
    <button class="btn">Delete</button>
</form>
